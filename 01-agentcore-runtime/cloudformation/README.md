# cloudformation — not started

Plan: raw, hand-authored YAML/JSON CloudFormation template using the native
`AWS::BedrockAgentCore::Runtime` resource type (added Sept 2025). No CDK involved — this is the
"read/write CloudFormation directly" skill, distinct from CDK even though CDK compiles down to
CloudFormation under the hood.
