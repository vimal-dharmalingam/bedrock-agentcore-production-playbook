# 01 - AgentCore Runtime (multiple deployment mechanisms, same target)

All subfolders here deploy the exact same calculator agent to the exact same target —
Amazon Bedrock AgentCore Runtime — using different tools. The point is to compare deployment
mechanics directly, not to build different agents.

## Subfolders

| Folder | Status | What it shows |
|---|---|---|
| `starter-toolkit-cli/` | Done (deployed, invoked) | Python starter toolkit (`agentcore configure`/`launch`) — deprecated by AWS, but the original working path, worth documenting for the tooling-shift story |
| `boto3-direct/` | Done (deployed, invoked) | Programmatic deploy/invoke via `bedrock_agentcore_starter_toolkit` SDK + raw `invoke_agent_runtime` boto3 call — no CLI at all |
| `scripts/` | Done | Reusable management utilities: `list_agents.py`, `delete_agent.py` — not a deployment method, just tooling used across this whole module |
| `direct-code-zip/` | Not started | Zip-upload deploy path (`CodeConfiguration`), no Docker/ECR at all |
| `manual-container-build/` | Not started | Your own `Dockerfile`, manual `docker build`/`push` to ECR, then `create_agent_runtime` pointed at that image — no CodeBuild automation |
| `cdk/` | Not started | Hand-written CDK (not auto-generated by a CLI) |
| `terraform/` | Not started | AWS's AgentCore Terraform provider |
| `cloudformation/` | Not started | Raw, hand-authored YAML/JSON template |

(The new npm-based `agentcore` CLI, which also deploys here via auto-generated CDK, lives in
the separate top-level `02-agentcore-cli/` folder, not nested under here, since it's a
different generation of tooling worth comparing side by side rather than nesting.)

## Note on duplication
`my_calc_agent.py` + `requirements.txt` are intentionally copied into both `starter-toolkit-cli/`
and `boto3-direct/` (and will be into each future sub-method folder too). Same agent code
everywhere, on purpose — each folder needs to be runnable standalone without reaching into a
sibling folder, and duplication makes that possible at the cost of needing to update the file in
more than one place if the agent logic ever changes.

## How to rerun / verify what's already deployed

```bash
cd starter-toolkit-cli
agentcore status
```
If this doesn't resolve cleanly after the folder reorg (state file was moved from the project
root into this subfolder), rerun `agentcore configure -e my_calc_agent.py` from this folder to
regenerate the local config — the actual deployed AWS resources are unaffected by moving local
files, only the local pointer might need refreshing.

```bash
cd boto3-direct
python invoke_agent.py
```

## Status
- [x] `starter-toolkit-cli` — deployed and invoked successfully (see notes below re: model IDs,
      IAM permissions, Marketplace subscription — all resolved)
- [x] `boto3-direct` — working
- [x] `scripts` — `list_agents.py`/`delete_agent.py` used to find and remove a duplicate agent
- [ ] `direct-code-zip`
- [ ] `manual-container-build`
- [ ] `cdk`
- [ ] `terraform`
- [ ] `cloudformation`

## Notes / gotchas (from building `starter-toolkit-cli` and `boto3-direct`)
- Old Claude 3.5 Haiku model ID went Legacy/EOL on Bedrock — had to switch to the current
  `us.anthropic.claude-haiku-4-5-20251001-v1:0` inference profile ID.
- One-time Anthropic "use case details" form required per AWS account before first invoke of
  any Anthropic model (separate from model access itself). Console location moved after AWS
  retired the old "Model access" page (Sept 2025) — now under Model Catalog.
- `AccessDeniedException` on first invoke: IAM user needs `aws-marketplace:Subscribe`/
  `ViewSubscriptions`/`Unsubscribe` permissions, since Bedrock auto-subscribes third-party
  models via AWS Marketplace on first use.
- `always_learner` IAM user needed a dedicated policy (`BedrockAgentCoreCLIAccess`) beyond
  `BedrockAgentCoreFullAccess` to actually run the CLI (ECR create, CodeBuild, S3, scoped IAM
  role management) — see `runtime-permissions.html` in AWS docs, this is the "starter toolkit
  policy" the original README references.
- Inline IAM policies have a 2048 non-whitespace character limit shared across all inline
  policies on a user — hit this creating the above policy, switched to a customer-managed
  policy instead (6144 char limit, separate quota).
- Windows `cmd.exe` doesn't support bash's `\` line continuation or single-quoted JSON strings
  — any curl/CLI example copied from Linux/Mac docs needs to be flattened to one line with
  escaped double quotes (`"{\"prompt\": \"...\"}"`).
- Duplicate agent runtime accidentally created — used `list_agents.py`/`delete_agent.py`
  (`bedrock-agentcore-control` boto3 client) to find and remove it, since the AWS CLI version
  installed locally initially predated that service.
