# cdk — not started

Plan: hand-written CDK (TypeScript or Python) defining the AgentCore Runtime resource directly
— not auto-generated by the `agentcore` CLI (compare against `02-agentcore-cli`, which uses CDK
under the hood but you never write or see it). Use `aws-cdk-lib.aws_bedrockagentcore`.
