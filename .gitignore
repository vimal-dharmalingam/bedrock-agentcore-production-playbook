# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Node / npm / CDK
node_modules/
cdk.out/

# AgentCore local state (contains account-specific values, not secrets, but noisy in diffs)
.bedrock_agentcore/

# Env / secrets
.env
.env.local
*.pem
*.key
.Readme.txt

# Build artifacts (arm64 wheels / packaged zips -- regenerated by build scripts, not source)
vendor/
deployment_package/
deployment_package.zip

# OS
.DS_Store
Thumbs.db

# Leftover empty folder from initial setup
deployment/
