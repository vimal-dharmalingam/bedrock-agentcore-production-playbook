# 07 - Inbound & Outbound Auth

**Status:** Not started

Who can invoke the agent (inbound) vs what the agent can act on (outbound) -- the mechanism-level version of what the full AgentCore Identity module covers.

## What to cover here

- [ ] IAM-based inbound auth
- [ ] OAuth-based inbound auth via a real corporate IdP (Okta/Entra ID/Cognito)
- [ ] Outbound auth to a real third-party API (OAuth or API key) -- candidate for the real MCP/API example
- [ ] How this differs from the dedicated 13-agentcore-identity module

## Notes

(Fill in as this is built -- real code, real errors, real fixes, same discipline as every other module in this repo.)
