# 01 - Framework & Model Flexibility

**Status:** Not started

Prove the "any framework, any model" claim: same Runtime deployment pattern, swap agent frameworks (Strands, LangGraph, CrewAI) and swap the underlying model without changing the hosting mechanics.

## What to cover here

- [ ] Deploy the same entrypoint contract with 2+ different agent frameworks
- [ ] Swap the underlying model with no Runtime-side changes
- [ ] Document exactly which lines of code change vs. stay identical

## Notes

(Fill in as this is built -- real code, real errors, real fixes, same discipline as every other module in this repo.)
