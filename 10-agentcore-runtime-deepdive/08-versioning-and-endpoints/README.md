# 08 - Versioning & Endpoints

**Status:** Not started

DEFAULT endpoint auto-advancing on redeploy, named endpoints, and blue-green style rollout patterns.

## What to cover here

- [ ] Deploy v1, then v2, observe DEFAULT endpoint auto-advance
- [ ] Create a named custom endpoint
- [ ] Point traffic deliberately between versions (blue-green pattern)

## Notes

(Fill in as this is built -- real code, real errors, real fixes, same discipline as every other module in this repo.)
